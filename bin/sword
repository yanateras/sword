#!/usr/bin/env ruby
# encoding: utf-8

module Sword
  require 'optparse'
  require File.absolute_path File.dirname(__FILE__) + '/../lib/constants'

  OptionParser.new do |o|
    o.summary_width = 17
    o.on('-b', '--build', 'Build current project into .zip') { require LIBRARY + '/build' }
    o.on('--debug', 'Run server noisily') { $debug = true }
    o.on('-d', '--dir <name>', 'Specify project directory') { |d| $directory = d }
    o.on('-g', '--gem <name>', 'Add a gem to require') { |g| open(REQUIRED, 'a') { |f| f.puts g }
      puts "`#{g}` will be loaded next time you run Sword."; exit }
    o.on('-h', '--help', 'Print this message') { puts o; exit }
    o.on('-i', '--install', 'Install must-have gems') { exec 'gem install ' + SETTINGS[:gems].map { |n| n.respond_to?(:first) ? n.last : n } * ' '; exit }
    o.on('-p', '--port <num>', 'Change the port (1111 by default)') { |p| $port = p }
    o.on('-v', '--version', 'Print Sword’s version') { puts 'Sword ' + VERSION; exit }
  end.parse!

  require LIBRARY + '/application'
  Application.run!
end
