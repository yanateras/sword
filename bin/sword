#!/usr/bin/env ruby
require 'psych'

$dir = File.dirname(__FILE__) + '/../lib'
$engine = Psych.load_file "#{$dir}/engine.yml"

require "#{$dir}/talk"
begin ARGV[0].nil? ? Talk.run : Talk.send(ARGV[0].gsub('-', ''))
rescue NoMethodError, ArgumentError; Talk.gem ARGV end
