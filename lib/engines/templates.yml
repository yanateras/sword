# Format is as follows:
# :: String    Both engine method and the only extension,
# :: Hash      Key is the engine method and value is an array of extensions 

# TODO:
# For each file in /engines folder Sword::Application has an instance variable,
# which contains a parsed YAML array of engines.

- haml
- liquid
- textile
- rdoc
- slim
- markaby:
  - mab
- markdown:
  - markdown
  - mkd
  - md
- erb:
  - erb
  - rhtml
  - erubis
